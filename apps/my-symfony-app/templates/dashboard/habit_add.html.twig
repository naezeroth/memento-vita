{% extends 'base_1.html.twig' %}

{%  block main %}

    <div class="example-wrapper">
        {{ form_start(habitForm) }}
        {{ form_row(habitForm.name) }}
        {{ form_row(habitForm.description) }}
        {{ form_row(habitForm.dateStart) }}
        {{ form_row(habitForm.freq) }}
        {{ form_row(habitForm.interval) }}
        {{ form_row(habitForm.count) }}
        {{ form_row(habitForm.goal) }}
        <div class="form-group row">
            <div class="col-sm-2"></div>
            <div class="col-sm-10">
                <input type="submit" value="Send" class="btn btn-success"/>
            </div>
        </div>
        {{ form_end(habitForm) }}
    </div>

{%  endblock %}